apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: syncthing
  namespace: flux-system
spec:
  interval: 10m
  path: ./apps/syncthing
  prune: true
  targetNamespace: syncthing
  # dependsOn:
  #   - name: cert-manager
  sourceRef:
    kind: GitRepository
    name: yggdrasil
